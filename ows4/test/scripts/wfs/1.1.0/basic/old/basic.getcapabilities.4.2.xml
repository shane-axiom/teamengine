<?xml version="1.0" encoding="UTF-8"?>
<ctl:package
 xmlns="http://www.occamlab.com/ctl"
 xmlns:xsl="http://www.w3.org/1999/XSL/Transform"
 xmlns:ctl="http://www.occamlab.com/ctl"
 xmlns:parsers="http://www.occamlab.com/te/parsers"
 xmlns:myparsers="http://www.galdosinc.com/myparsers"
 xmlns:saxon="http://saxon.sf.net/"
 xmlns:wfs="http://www.opengis.net/wfs"
 xmlns:xi="http://www.w3.org/2001/XInclude"
 xmlns:xsd="http://www.w3.org/2001/XMLSchema"
 xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
 xsi:schemaLocation="http://www.occamlab.com/ctl ../../../../../../apps/engine/resources/com/occamlab/te/schemas/ctl.xsd">

	<test name="wfs:basic.getcapabilities.4.2">
      <param name="VAR_WFS_GET_CAPABILITIES_HTTP_POST_URL"/>
      <assertion>When a wfs:GetCapabilities request is made without specifying a version number, the result document must be a wfs:WFS_Capabilities with the version number equal to the highest version number that it supports.</assertion>
      <comment>This test issues a GetCapabilities request using HTTP POST to the WFS server under test. The request will noy define a version number. The response document will be tested to check that the version number returned in the capabilities document is greater than 0.0.0 (i.e. a version number was specified).</comment>
      <link>wfs-1.1.0#basic.getcapabilities.post.4</link>
		<code>
		
			<xsl:variable name="request1">
				<request>
				   <url>
					  <xsl:value-of select="$VAR_WFS_GET_CAPABILITIES_HTTP_POST_URL"/>
				   </url>
				   <method>post</method>
				   <body>&lt;wfs:GetCapabilities
  service="WFS"
  xmlns:wfs="http://www.opengis.net/wfs"
/&gt;
					</body>
					<myparsers:XMLValidatingParser.WFS/>
				</request>
			</xsl:variable>
			
			<xsl:if test="not($request1/*)">
				<ctl:fail/>
			</xsl:if>

			<xsl:if test="$request1/*">
				<xsl:variable name="expression">number(/*/@version) &gt; number('0.0.0')</xsl:variable>
				<ctl:call-test name="ctl:assert-xpath">
					<ctl:with-param name="expr" select="$expression"/>
					<ctl:with-param name="doc" select="$request1"/>
				</ctl:call-test>
			</xsl:if>
			
		</code>
	</test>
</ctl:package>
