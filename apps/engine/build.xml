<project name="team" basedir="." default="jar">
	<description>Main TEAM Engine application</description>
	
	<!-- The build.properties file(s) must contain the path location of the Java JRE and Tomcat installation-->
	<property file="../../build.properties"/>
	<property file="build.properties"/>
	
	<property name="lib.dir" value="${basedir}/lib"/>
	<property name="resource.dir" value="${basedir}/resources"/>
	<property name="test.dir" value="${basedir}/../../test"/>
	
	<path id="classpath">
		<fileset dir="lib" includes="*.jar" excludes="teamengine.jar"/>
	</path>
	
	<target name="compile">
		<mkdir dir="build"/>
		<javac srcdir="src/java" destdir="build" classpathref="classpath" debug="true"/>
	</target>
	
	<target name="jar" depends="compile">
		<jar destfile="lib/teamengine.jar">
			<fileset dir="build" excludes=".*"/>
		</jar>
	</target>
	
	<target name="javadoc" description="Creates javadocs from the java source">
		<mkdir dir="build"/>
		<javadoc destdir="build/javadocs" author="true" version="true" use="true" windowtitle="Teamengine API">
			<fileset dir="${basedir}/src/java/com/occamlab/te" defaultexcludes="yes"/>
		</javadoc>
	</target>
	
	<target name="clean">
		<delete dir="build"/>
	</target>
	
	<!--Usage: ant test -Dquery="-mode=test -source=../../test/scripts/wfs/wfs.ctl -logdir=logs" -->
	<target name="test" if="query" description="Runs the main Test class with supplied argument string">
		<java classname="com.occamlab.te.Test" fork="true" jvm="${java.dir}/bin/java.exe">
			<env key="JAVA_HOME" value="${java.dir}"/>
			<arg line="${query}"/>
			<classpath>
				<fileset dir="${lib.dir}">
					<include name="**/*.jar"/>
				</fileset>
				<pathelement path="${resource.dir}"/>
				<pathelement path="${java.class.path}"/>
				<pathelement path="${basedir}"/>
				<pathelement path="${test.dir}/resources"/>
				<pathelement path="${test.dir}/scripts"/>
			</classpath>
		</java>
	</target>
	
	<!--Usage: ant listsuites -Dquery="-source=../../test/scripts/temp/demo.xml" -->
	<target name="listsuites" if="query" description="Lists the suites in a ctl test file">
		<java classname="com.occamlab.te.ListSuites" fork="true" jvm="${java.dir}/bin/java.exe">
			<env key="JAVA_HOME" value="${java.dir}"/>
			<arg line="${query}"/>
			<classpath>
				<fileset dir="${lib.dir}">
					<include name="**/*.jar"/>
				</fileset>
				<pathelement path="${resource.dir}"/>
				<pathelement path="${java.class.path}"/>
			</classpath>
		</java>
	</target>
	
	<!--Usage: ant viewlog -Dquery="-logdir=logs -session=s0001" -->
	<target name="viewlog" if="query" description="Views the log results from a test run">
		<java classname="com.occamlab.te.ViewLog" fork="true" jvm="${java.dir}/bin/java.exe">
			<env key="JAVA_HOME" value="${java.dir}"/>
			<arg line="${query}"/>
			<classpath>
				<fileset dir="${lib.dir}">
					<include name="**/*.jar"/>
				</fileset>
				<pathelement path="${resource.dir}"/>
				<pathelement path="${java.class.path}"/>
			</classpath>
		</java>
	</target>
	
</project>
