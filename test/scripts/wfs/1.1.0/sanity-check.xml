<?xml version="1.0" encoding="UTF-8"?>
<ctl:package
 xmlns="http://www.occamlab.com/ctl"
 xmlns:xsl="http://www.w3.org/1999/XSL/Transform"
 xmlns:ctl="http://www.occamlab.com/ctl"
 xmlns:parsers="http://www.occamlab.com/te/parsers"
 xmlns:myparsers="http://www.galdosinc.com/myparsers"
 xmlns:saxon="http://saxon.sf.net/"
 xmlns:wfs="http://www.opengis.net/wfs"
 xmlns:xi="http://www.w3.org/2001/XInclude"
 xmlns:xsd="http://www.w3.org/2001/XMLSchema"
 xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
 xsi:schemaLocation="http://www.occamlab.com/ctl ../../../../apps/engine/resources/com/occamlab/te/schemas/ctl.xsd">

	<test name="wfs:sanity-check">
      <param name="VAR_WFS_GET_CAPABILITIES_HTTP_GET_URL"/>
      <param name="VAR_WFS_GET_FEATURE_HTTP_POST_URL"/>
      <assertion>WFS sanity check - to determine if the service is running and has the test data loaded.</assertion>
      <comment>This test first issues a GetCapabilities request using HTTP GET to the WFS server under test to ensure the service is running.  It then performs a GetFeature request for one of the data members that must be loaded to the server.</comment>
		<code>
		
			<xsl:variable name="request1">
				<request>
					<url>
						<xsl:value-of select="$VAR_WFS_GET_CAPABILITIES_HTTP_GET_URL"/>
					</url>
					<method>get</method>
					<param name="service">WFS</param>
					<param name="version">1.1.0</param>
					<param name="request">GetCapabilities</param>
					<myparsers:XMLValidatingParser.WFS/>
				</request>
			</xsl:variable>
			
			<xsl:if test="not($request1/*)">
				<ctl:fail/>
			</xsl:if>

			<xsl:if test="$request1/*">
				<xsl:variable name="expression">//wfs:WFS_Capabilities</xsl:variable>
				<ctl:call-test name="ctl:assert-xpath">
					<ctl:with-param name="expr" select="$expression"/>
					<ctl:with-param name="doc" select="$request1"/>
				</ctl:call-test>
			</xsl:if>

			<xsl:variable name="request2">
				<request>
					<url>
						<xsl:value-of select="$VAR_WFS_GET_FEATURE_HTTP_POST_URL"/>
					</url>
					<method>post</method>
				    <body><![CDATA[
				    <wfs:GetFeature xmlns:wfs="http://www.opengis.net/wfs" version="1.1.0" service="WFS">
					    <wfs:Query xmlns:sf="http://www.opengis.net/cite/gmlsf" typeName="sf:SimplePointFeature">
					    	<ogc:Filter>
								<ogc:PropertyIsEqualTo>
									<ogc:PropertyName>@gmlid</ogc:PropertyName>
									<ogc:Literal>f002</ogc:Literal>
								</ogc:PropertyIsEqualTo>
							</ogc:Filter>
						</wfs:Query>	
				    </wfs:GetFeature>
				    ]]>
					</body>
					<myparsers:XMLValidatingParser.WFS/>
				</request>
			</xsl:variable>
			
			<xsl:if test="not($request2/*)">
				<ctl:fail/>
			</xsl:if>

			<xsl:if test="$request2/*">
				<xsl:variable name="expression">count(//wfs:FeatureCollection/*) = 1</xsl:variable>
				<ctl:call-test name="ctl:assert-xpath">
					<ctl:with-param name="expr" select="$expression"/>
					<ctl:with-param name="doc" select="$request2"/>
				</ctl:call-test>
			</xsl:if>
				
		</code>
	</test>
</ctl:package>
